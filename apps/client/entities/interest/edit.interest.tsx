
import { 
  Edit,
  SimpleForm,
  
} from "react-admin";

// -------------------------------------------------------------

export default function EditInterest(props: any) {
return (
    <Edit>
      <SimpleForm redirect="list">
        
      </SimpleForm>
    </Edit>
  );
}